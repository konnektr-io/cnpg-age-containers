name: Build and Publish CNPG + Apache AGE images

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - pg_major: '16'
            age_version: '1.5.0'
            cnpg_variant: standard-bookworm
            cnpg_postgis_variant: 3-standard-bookworm
          - pg_major: '16'
            age_version: '1.5.0'
            cnpg_variant: standard-trixie
            cnpg_postgis_variant: 3-standard-trixie
          - pg_major: '16'
            age_version: '1.6.0'
            cnpg_variant: standard-bookworm
            cnpg_postgis_variant: 3-standard-bookworm
          - pg_major: '16'
            age_version: '1.6.0'
            cnpg_variant: standard-trixie
            cnpg_postgis_variant: 3-standard-trixie
          - pg_major: '17'
            age_version: '1.6.0'
            cnpg_variant: standard-bookworm
            cnpg_postgis_variant: 3-standard-bookworm
          - pg_major: '17'
            age_version: '1.6.0'
            cnpg_variant: standard-trixie
            cnpg_postgis_variant: 3-standard-trixie
          # Only build timescale variants for PG 17, AGE 1.6.0, standard-trixie
          - pg_major: '17'
            age_version: '1.6.0'
            cnpg_variant: standard-trixie
            cnpg_postgis_variant: 3-standard-trixie
            timescale_version: 2.19.3

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up build metadata (base)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/age
          tags: |
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }}-${{ matrix.cnpg_variant }}
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }},enable=${{ matrix.cnpg_variant == 'standard-trixie' }}
          labels: |
            org.opencontainers.image.title=Apache AGE for CNPG
            org.opencontainers.image.description=PostgreSQL ${{ matrix.pg_major }} with Apache AGE ${{ matrix.age_version }} on CloudNativePG base image (variant ${{ matrix.cnpg_variant }})
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=Apache-2.0

      - name: Set up build metadata (pgvector)
        id: meta_pgvector
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/age-pgvector
          tags: |
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }}-${{ matrix.cnpg_variant }}
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }},enable=${{ matrix.cnpg_variant == 'standard-trixie' }}
          labels: |
            org.opencontainers.image.title=Apache AGE + pgvector for CNPG
            org.opencontainers.image.description=PostgreSQL ${{ matrix.pg_major }} with Apache AGE ${{ matrix.age_version }} and pgvector on CloudNativePG base image (variant ${{ matrix.cnpg_variant }})
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=Apache-2.0

      - name: Set up build metadata (pgvector-postgis)
        id: meta_pgvector_postgis
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/age-pgvector-postgis
          tags: |
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }}
          labels: |
            org.opencontainers.image.title=Apache AGE + pgvector + PostGIS for CNPG
            org.opencontainers.image.description=PostgreSQL ${{ matrix.pg_major }} with Apache AGE ${{ matrix.age_version }}, pgvector, and PostGIS
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=Apache-2.0

      - name: Set up build metadata (pgvector-timescale)
        id: meta_pgvector_timescale
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/age-pgvector-timescale
          tags: |
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }}-${{ matrix.cnpg_variant }}
          labels: |
            org.opencontainers.image.title=Apache AGE + pgvector + TimescaleDB for CNPG
            org.opencontainers.image.description=PostgreSQL ${{ matrix.pg_major }} with Apache AGE ${{ matrix.age_version }}, pgvector, and TimescaleDB
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=Apache-2.0

      - name: Set up build metadata (pgvector-postgis-timescale)
        id: meta_pgvector_postgis_timescale
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/age-pgvector-postgis-timescale
          tags: |
            type=raw,value=${{ matrix.pg_major }}-${{ matrix.age_version }}
          labels: |
            org.opencontainers.image.title=Apache AGE + pgvector + PostGIS + TimescaleDB for CNPG
            org.opencontainers.image.description=PostgreSQL ${{ matrix.pg_major }} with Apache AGE ${{ matrix.age_version }}, pgvector, PostGIS, and TimescaleDB
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=Apache-2.0

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (base)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            PG_MAJOR=${{ matrix.pg_major }}
            AGE_VERSION=${{ matrix.age_version }}
            CNPG_VARIANT=${{ matrix.cnpg_variant }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false

      - name: Build and push (pgvector)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.age-pgvector
          push: true
          build-args: |
            PG_MAJOR=${{ matrix.pg_major }}
            AGE_VERSION=${{ matrix.age_version }}
            CNPG_VARIANT=${{ matrix.cnpg_variant }}
          tags: ${{ steps.meta_pgvector.outputs.tags }}
          labels: ${{ steps.meta_pgvector.outputs.labels }}
          provenance: false

      - name: Build and push (pgvector-postgis)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.age-pgvector-postgis
          push: true
          build-args: |
            PG_MAJOR=${{ matrix.pg_major }}
            AGE_VERSION=${{ matrix.age_version }}
            CNPG_POSTGIS_VARIANT=${{ matrix.cnpg_postgis_variant }}
          tags: ${{ steps.meta_pgvector_postgis.outputs.tags }}
          labels: ${{ steps.meta_pgvector_postgis.outputs.labels }}
          provenance: false

      - name: Build and push (pgvector-timescale)
        if: ${{ matrix.timescale_version != '' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.age-pgvector-timescale
          push: true
          build-args: |
            PG_MAJOR=${{ matrix.pg_major }}
            AGE_VERSION=${{ matrix.age_version }}
            CNPG_VARIANT=${{ matrix.cnpg_variant }}
            TIMESCALE_VERSION_ARG=${{ matrix.timescale_version }}
          tags: ${{ steps.meta_pgvector_timescale.outputs.tags }}
          labels: ${{ steps.meta_pgvector_timescale.outputs.labels }}
          provenance: false

      - name: Build and push (pgvector-postgis-timescale)
        if: ${{ matrix.timescale_version != '' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.age-pgvector-postgis-timescale
          push: true
          build-args: |
            PG_MAJOR=${{ matrix.pg_major }}
            AGE_VERSION=${{ matrix.age_version }}
            CNPG_POSTGIS_VARIANT=${{ matrix.cnpg_postgis_variant }}
            TIMESCALE_VERSION_ARG=${{ matrix.timescale_version }}
          tags: ${{ steps.meta_pgvector_postgis_timescale.outputs.tags }}
          labels: ${{ steps.meta_pgvector_postgis_timescale.outputs.labels }}
          provenance: false

      - name: Summary
        run: |
          echo "Published image tags (base):"
          echo "${{ steps.meta.outputs.tags }}"
          echo "Published image tags (pgvector):"
          echo "${{ steps.meta_pgvector.outputs.tags }}"
          echo "Published image tags (pgvector-postgis):"
          echo "${{ steps.meta_pgvector_postgis.outputs.tags }}"
          echo "Published image tags (pgvector-timescale):"
          echo "${{ steps.meta_pgvector_timescale.outputs.tags }}"
          echo "Published image tags (pgvector-postgis-timescale):"
          echo "${{ steps.meta_pgvector_postgis_timescale.outputs.tags }}"